<%= form_for(@blog_tag) do |f| %>
  <% if @blog_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blog_tag.errors.count, "error") %> prohibited this blog_tag from being saved:</h2>

      <ul>
      <% @blog_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
